import { createRouter, createWebHistory } from 'vue-router' //createWebHashHistory去掉中间的hash字母


const routes = [
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

export default router
